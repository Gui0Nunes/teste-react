const initialState = {
    usuarios:[
        'Gui',
        'Murilo',
        'Lucas',
        'Matheus'
    ]
}

const usuariosReducer = (state = initialState.usuarios) => {
    return state;
}

export default usuariosReducer;